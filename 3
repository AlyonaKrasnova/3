#include <iostream>
using namespace std;
void NeProsto(int x, int y)
{
    if(x==1)
        return;
    for (;;y++)
        if(x%y==0)
        {
            cout<<y<<endl;
            NeProsto(x/y, y);
            return;
        }
}
int main()
{
    int a, k = 2;
    cin>>a;
    NeProsto(a, k);
}
